# Build-time secret registry. Values: optional, required, required-release. Non-empty values need 8+ UTF-8 bytes.
# Add NAME=policy, matching GitHub Actions env, then call BuildSecrets.NAME().
# Local values belong in ignored application-local.properties.
LOGGLY_TOKEN=optional
DCAM_CRYPTO_PASSWORD=required-release
